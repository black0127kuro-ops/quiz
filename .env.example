# サイト全体のパスワード（未設定ならロックなし・ローカル開発向け）
SITE_PASSWORD=your-password-here

# セッション署名用（本番ではランダムな長い文字列に変更推奨）
# AUTH_SECRET=change-this-to-a-long-random-string

# 本番（Render 等の HTTPS プロキシの背後）
# NODE_ENV=production
# TRUST_PROXY=1

# ポート（ホスティングサービスが PORT を渡す場合はそのまま）
# PORT=3000
