# USB 版（校務PCにインストール不要）

校務PCに **Node.js をインストールせず** に使う方法です。

---

## 2つの形

| 方式 | 中身 | 校務PCでの操作 |
|------|------|----------------|
| **USB フォルダ版（推奨・確実）** | フォルダ1つ（Node 同梱） | **`quiz-lan.bat`** をダブルクリック |
| exe 1本版 | 単一 `.exe` | 開発環境によってはビルドが難しいため、**フォルダ版を推奨** |

どちらも **「インストーラーで Node を入れる」必要はありません。**  
USB から動かすだけです。

---

## 準備（自宅PCなどで1回）

1. **`build-usb.bat`** をダブルクリック  
2. **`dist\quiz-lan-usb`** フォルダができる  
3. この **フォルダごと** USB にコピー  

フォルダの中身:

```
quiz-lan-usb/
  quiz-lan.bat    ← 校務PCでこれをダブルクリック
  使い方.txt
  node/           ← ポータブル Node（インストール不要）
  app/            ← クイズ本体
```

---

## 校務PC（クイズの日）

1. USB を差す  
2. **`quiz-lan.bat`** をダブルクリック  
3. 黒い窓に URL が表示される  
4. このPCのブラウザで **`http://localhost:3000`** → 部屋を作る  
5. 参加者に **`http://172.25.xx.xx:3000`**（表示された IPv4）と部屋番号を伝える  
6. 終わったら黒い窓を閉じる  

---

## 参加者

- ブラウザだけ（bat も Node も不要）  
- 主催者から伝えられた URL を開く  

---

## exe 1本について

「`quiz-lan.exe` だけ USB に入れたい」場合、Windows ではビルドに追加ツールが必要なことがあります。  
**機能は同じ** なので、まずは **`quiz-lan-usb` フォルダ版** を使うのが確実です。

フォルダ版でも:

- 校務PCに **Node インストール不要**  
- **アンインストール作業も不要**（USB を抜くだけ）  
- 中身は exe 案と同じ（Node + クイズ）

---

## 注意

- **Windows 64bit** の校務PC  
- 初回 **ファイアウォールで許可**  
- 学校で **USB 実行が禁止** されていないこと  
- フォルダサイズは **約 80〜100MB**（Node 同梱のため）
