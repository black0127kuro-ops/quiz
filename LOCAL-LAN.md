# ローカルネットワーク版（LAN）の使い方

授業室・自宅Wi‑Fiなど **同じネットワーク内だけ** で使う方法です。  
**quiz-kuro.com などインターネット公開は不要** で、iFilter 等の「未確認サイト」判定の影響を受けにくくなります。

---

## 必要なもの

| 役割 | 端末 |
|------|------|
| 主催者 | パソコン1台（Node.js 18+ をインストール） |
| 参加者 | 同じ Wi‑Fi / LAN に繋がったスマホ・PC |

**IPv4 アドレス**（例: `192.168.0.12`）が分かれば十分です。  
主催者PCの IPv4 を参加者に伝え、全員が同じURLを開きます。

---

## 手順

### 1. 主催者PCで起動

```powershell
cd "C:\Users\user\Documents\quiz HP"
npm install
npm start
```

または **`start-lan.bat`** をダブルクリック。

ターミナルに例えば次のように表示されます:

```
  このPC:     http://localhost:3000
  同じWi‑Fi/LANの端末からは次のURLで開けます:
    http://192.168.0.12:3000  (Wi-Fi)
```

### 2. 主催者

- このPCのブラウザで `http://localhost:3000` を開く
- 「部屋をつくる」→ 4桁の部屋番号が出る

### 3. 参加者

- **主催者から伝えられた IPv4** を使う  
  例: `http://192.168.0.12:3000`
- 部屋番号 + ニックネームで参加

---

## IPv4 の調べ方（Windows）

```powershell
ipconfig
```

**「ワイヤレス LAN アダプター Wi‑Fi」** または **「イーサネット」** の  
**IPv4 アドレス**（`192.168.x.x` や `10.x.x.x` など）を使います。

---

## うまく繋がらないとき

1. **全員が同じ Wi‑Fi / 同じLANか** 確認（ゲストWi‑Fiは端末同士が見えないことがあります）
2. **主催者PCの Windows ファイアウォール**  
   初回起動時「プライベートネットワークで許可」が出たら **許可**
3. 主催者PCを **スリープさせない**（サーバーが止まると部屋も消えます）
4. URL は **`https` ではなく `http`**（ポート `:3000` 付き）

---

## 公開サイト版との違い

| | 公開版 (quiz-kuro.com) | LAN版 (npm start) |
|--|------------------------|-------------------|
| インターネット | 必要 | **不要** |
| 主催者PC | 常時起動不要 | **起動中だけ有効** |
| 参加者のURL | ドメイン | `http://192.168.x.x:3000` |
| 問題セット | ブラウザ保存 | 同じ（ブラウザ保存） |

コードは同じです。**起動の仕方が違うだけ** です。

---

## 校務PCにインストールしたくない場合

**`build-usb.bat`** で `dist\quiz-lan-usb` フォルダを作り、USB に入れます。  
校務PCでは **`quiz-lan.bat`** をダブルクリックするだけ（Node インストール不要）。

詳しくは **`LOCAL-EXE.md`** を参照してください。

Chromebook を主催者にする手順は **`CHROMEBOOK-LAN.md`** を参照してください。
