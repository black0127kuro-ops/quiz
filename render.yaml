# Render Blueprint: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: quiz-buzzer-hp
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: TRUST_PROXY
        value: "1"
